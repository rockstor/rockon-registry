{
  "NetAlertX": {
    "description": "NetAlertX is an intrusion detection system (IDS). Easily visualize all your networks in one place, enhance security with real-time alerts, and enjoy seamless integration with your smart home.<p><b>Note:</b> uses the `net=host` option to enable local scanning.</p><p>Based on the <a href='https://hub.docker.com/r/jokobsk/netalertx' target='_blank'>official OCI image</a>, available for amd64 and arm64 architecture.</p>",
    "version": "1.0",
    "website": "https://netalertx.com/",
    "more_info": "<h4>NetAlertX</h4><p><b>Admin page</b></p><p>To access admin interface go to URL: http://[SERVERIP]:[Web-Port].</p>",
    "volume_add_support": true,
    "containers": {
      "netalertx": {
        "image": "jokobsk/netalertx",
        "launch_order": 1,
        "ports": {
          "20211": {
            "description": "Web-UI Link Port. Recommended: 20211. Must match the subsequent NetAlertX later in the configuration.",
            "label": "Web-UI Port [e.g. 20211]",
            "host_default": 20211,
            "ui": true
          }
        },
        "volumes": {
          "/app/config": {
            "description": "Share for NetAlertX configuration.",
            "label": "NetAlertX config [e.g. netalertx-config]"
          },
          "/app/db": {
            "description": "Share for NetAlertX database.",
            "label": "NetAlertX DB [e.g. netalertx-db]"
          }
        },
        "opts": [
          [
            "--net",
            "host"
          ]
        ],
        "environment": {
          "PORT": {
            "description": "NetAlertX Port. Must match Web-UI Port, configured earlier. Ensures admin pages for NetAlertX is Web Browser accessible.",
            "label": "NetAlertX Port [e.g. 20211]",
            "index": 1
          }
        }
      }
    }
  }
}
