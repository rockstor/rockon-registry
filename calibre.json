{
  "Calibre": {
    "description": "Calibre is a powerful and easy to use e-book manager. Users say it's outstanding and a must-have. It'll allow you to do nearly everything and it takes things a step beyond normal e-book software. It's also completely free and open source and great for both casual users and computer experts.</p><p>Calibre can be accessed using http (default), https, or through a simple built-in web server (see documentation how to activate within the app and note the web UI port required in the Rockon configuration).</p><p>Based on a <a href='https://hub.docker.com/r/linuxserver/Calibre' target='_blank'>custom OCI image</a>, available for amd64 and arm64 architecture.</p>",
    "version": "1.0",
    "website": "https://Calibre-ebook.com/",
    "icon": "https://github.com/kovidgoyal/Calibre/blob/master/icons/favicon.ico",
    "containers": {
      "calibreserver": {
        "image": "linuxserver/calibre",
        "launch_order": 1,
        "ports": {
          "8080": {
            "description": "Calibre Desktop (Backend) Access using http.",
            "label": "Desktop port [e.g 8080]",
            "host_default": 8080,
            "protocol": "tcp",
            "ui": true
          },
          "8081": {
            "description": "Calibre Web Server access port.",
            "label": "Web Server port [e.g. 8081]",
            "host_default": 8081,
            "protocol": "tcp"
          },
          "8181": {
            "description": "Calibre Desktop (Backend) Access using https.",
            "label": "Desktop port [e.g. 8181]",
            "host_default": 8181,
            "protocol": "tcp"
          }
        },
        "volumes": {
          "/config": {
            "description": "Share for the Calibre configuration.",
            "label": "Config Storage [e.g. Calibre-config]"
          }
        },
        "opts": [
          [
            "-e",
            "DOCKER_MODS=linuxserver/mods:universal-package-install"
          ],
          [
            "-e",
            "INSTALL_PACKAGES=fonts-noto-cjk"
          ]
        ],
        "environment": {
          "LC_ALL": {
            "description": "Which locale to start Calibre in, using utf8 (e.g., zh_CN.UTF-8). If in US English, enter 'en_US.UTF-8'. The language can still be adjusted within Calibre's startup wizard.",
            "label": "Locale installs [e.g. en_US.UTF-8]",
            "index": 3,
            "default": "en_US.UTF-8"
          },
          "PGID": {
            "description": "Enter a valid GID of an existing group with permission to the config share assigned above.",
            "label": "GID",
            "index": 2,
            "default": "1000"
          },
          "PUID": {
            "description": "Enter a valid UID of an existing user with permission to the config share assigned above.",
            "label": "UID",
            "index": 1,
            "default": "1000"
          }
        },
        "devices": {
          "/dev/dri": {
            "description": "(Optional) Path for mounting GPU into container (/dev/dri). Leave blank if not needed.",
            "label": "GPU device"
          }
        }
      }
    }
  }
}
